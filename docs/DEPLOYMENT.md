# Deployment

- Frontend: deploy `frontend` to Vercel/Netlify.
- Backend: deploy `backend` to Railway/Render/Fly.io.
- Set environment variables from `.env.example`.
- Configure CORS allow-list and HTTPS-only domains.
